# Railway sets PORT automatically. Use this only for local development.
PORT=3000

# Comma-separated list of allowed browser origins, for example:
# CORS_ORIGINS=https://your-vercel-app.vercel.app,https://www.your-domain.com
CORS_ORIGINS=

# Required when database-backed routes are added or when importing @workspace/db.
DATABASE_URL=
